package scala.basic.syntax

object ValAndVars {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(103); 
  println("The best way to use var is not to use them");$skip(68); 
  println("Try to avoid use of var in program as much as possible");$skip(107); 
                                                  
  println("Val is equivalent to final keyword in java");$skip(66); 
  println("use final keyword as much as you can in java as well");$skip(59); 
  println("Don't let var to escape out of function scope");$skip(46); 
  println("Always give back Immutable stuff");$skip(117); 
  
  println("If you look at scala library it takes mutable stuff but what it gives back to you is Immutable stuff")}
  
}
